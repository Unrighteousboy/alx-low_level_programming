#include "main.h"
/**
 * add - add two integers
 * @a: character to be checked
 * @b: character to be checked
 * Return: the result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
